# GitHub에 올릴 파일 / 올리지 말 파일

`github.com/...` 주소는 **코드 저장소**입니다. 거기에 `README.md`(명세서)가 크게 보이는 것이 정상입니다.  
로그인 화면이 나오는 **사이트**는 GitHub Pages가 아니라 **Streamlit Community Cloud**에서 `app.py`를 실행해야 합니다.

Main file path는 반드시 `app.py` 입니다. `README.md` 로 두면 명세서만 나옵니다.

---

## 올릴 것 (이 목록만)

```
app.py
requirements.txt
.gitignore
README.md
run.bat
pytest.ini

.streamlit/config.toml
.streamlit/secrets.toml.example

src/                    ← 폴더 통째로
static/atec_ci.png
tests/                  ← 없어도 사이트는 돌아감. 있어도 됨
```

로고 원본 `ATEC 영문_기본형.png`는 올려도 되고, `static/atec_ci.png`만 있어도 화면은 됩니다.

---

## 올리지 말 것

| 경로 | 이유 |
|---|---|
| `venv/` | 가상환경. 용량만 크고 Cloud가 이걸 실행하지 않음 |
| `.streamlit/secrets.toml` | 비밀번호·Gemini 키. Cloud **Secrets**에만 넣음 |
| `data/` , `*.db` | 로컬 DB. Cloud는 재시작 시 날아감 |
| `presentation/` | 발표 자료. 앱과 무관 |
| `__pycache__/`, `.pytest_cache/` | 임시 파일 |

GitHub에 `venv`를 통째로 올리면 배포가 실패하거나 엉뚱한 경로를 읽습니다.

---

## Streamlit Cloud에서 사이트 켜는 법

1. GitHub에 **위 목록만** 있는 저장소를 만듭니다. (이미 README만 있는 저장소면 그 위에 `app.py`와 `src`를 추가하세요.)
2. [share.streamlit.io](https://share.streamlit.io) → **New app**
3. Repository: 방금 올린 저장소
4. **Main file path: `app.py`**  ← 여기가 핵심
5. Deploy
6. 앱 설정 → **Secrets**에 로컬 `secrets.toml`과 같은 내용을 붙여 넣습니다.  
   (`GEMINI_API_KEY`, `cookie_key`, `[passwords]` 전부)

GitHub 저장소 페이지의 Settings → Pages는 켜지 마세요. Pages는 HTML 사이트용이라 명세서만 보입니다.

---

## Cloud Secrets 예시 모양

로컬 `.streamlit/secrets.toml`을 열어 그대로 복사하면 됩니다. Git에는 넣지 않습니다.

```toml
GEMINI_API_KEY = "발급키"
cookie_key = "긴-무작위-문자열"

[passwords]
prodadmin = "실제비밀번호"
director = "실제비밀번호"
jejo = "실제비밀번호"
gumae = "실제비밀번호"
quality = "실제비밀번호"
tech = "실제비밀번호"
jajae = "실제비밀번호"
```

---

## 실사용 안내

Community Cloud는 서버가 잠들면 SQLite가 초기화될 수 있습니다.  
팀 실사용은 사내 PC에서 `run.bat` (포트 8501)이 더 안전합니다. Cloud는 발표·데모용으로 쓰는 것이 맞습니다.
